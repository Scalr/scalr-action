{
  "name": "scalr-action",
  "version": "1.5.0",
  "description": "",
  "main": "index.js",
  "packageManager": "bun@1.3.4",
  "scripts": {
    "build": "bun build src/terraform.js --production --target=node --format=cjs --packages=bundle --outfile dist/terraform/index.js && bun build src/wrapper.js --production --target=node --format=cjs --packages=bundle --outfile dist/wrapper/index.js",
    "test": "node --test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Scalr/scalr-action.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Scalr/scalr-action/issues"
  },
  "homepage": "https://github.com/Scalr/scalr-action#readme",
  "dependencies": {
    "@actions/core": "^2.0.3",
    "@actions/io": "^2.0.0",
    "@actions/tool-cache": "^3.0.1"
  }
}
